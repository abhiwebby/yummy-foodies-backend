import userModel from "../models/userModel";

export const getUsers = (req, res) => {
  res.send("get all users data...");
};

export const addtUser = (req, res) => {
  res.send("add user data...");
};
